<!DOCTYPE html>
<html lang="it" data-bs-theme="dark">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>Asteroid Monitoring</title>

  <link rel="icon" href="data:,">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/sweetalert2@11.10.8/dist/sweetalert2.min.css" rel="stylesheet">

  <style>
    :root {
      --nasa-bg: #07090f;
      --nasa-panel: rgba(15, 18, 28, 0.92);
      --nasa-panel-soft: rgba(255, 255, 255, 0.055);
      --nasa-border: rgba(255, 255, 255, 0.11);
      --nasa-border-strong: rgba(110, 231, 255, 0.35);
      --nasa-text: #f4f7fb;
      --nasa-muted: #9ba7b7;
      --nasa-cyan: #6ee7ff;
      --nasa-blue: #3b82f6;
      --nasa-green: #22c55e;
      --nasa-orange: #f59e0b;
      --nasa-red: #ef4444;
      --nasa-purple: #a855f7;
      --nasa-radius: 18px;
    }

    html,
    body {
      font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
      font-size: 14px;
      line-height: 1.35;
    }

    body {
      min-height: 100vh;
      background:
        radial-gradient(circle at 75% 10%, rgba(59, 130, 246, 0.22), transparent 24%),
        radial-gradient(circle at 15% 25%, rgba(110, 231, 255, 0.10), transparent 18%),
        radial-gradient(circle at 82% 82%, rgba(168, 85, 247, 0.14), transparent 26%),
        linear-gradient(135deg, #03040a 0%, #080b12 48%, #0e111c 100%);
      color: var(--nasa-text);
      overflow-x: hidden;
    }

    body::before {
      content: "";
      position: fixed;
      inset: 0;
      pointer-events: none;
      background-image:
        radial-gradient(circle, rgba(255, 255, 255, .65) 0 1px, transparent 1.5px),
        radial-gradient(circle, rgba(110, 231, 255, .35) 0 1px, transparent 1.4px);
      background-size: 120px 120px, 210px 210px;
      background-position: 30px 40px, 80px 100px;
      opacity: .28;
      z-index: -1;
    }

    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
      letter-spacing: -0.02em;
    }

    .app-shell {
      width: min(1560px, calc(100% - 32px));
      margin: 0 auto;
      padding: 24px 0 40px;
    }

    .glass-card {
      background: var(--nasa-panel);
      border: 1px solid var(--nasa-border);
      border-radius: var(--nasa-radius);
      box-shadow: 0 20px 60px rgba(0, 0, 0, .22);
      backdrop-filter: blur(18px);
      transition: border-color .18s ease, box-shadow .18s ease;
    }

    .glass-card:hover {
      border-color: rgba(110, 231, 255, .18);
    }

    .dashboard-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 18px;
      margin-bottom: 18px;
    }

    .brand-block {
      display: flex;
      align-items: center;
      gap: 14px;
    }

    .brand-logo {
      width: 54px;
      height: 54px;
      border-radius: 17px;
      display: grid;
      place-items: center;
      background:
        radial-gradient(circle at 30% 25%, rgba(255, 255, 255, .65), transparent 18%),
        linear-gradient(135deg, var(--nasa-blue), var(--nasa-cyan));
      color: #031018;
      font-size: 25px;
      box-shadow: 0 0 35px rgba(110, 231, 255, .24);
      transform: rotate(-10deg);
    }

    .brand-title {
      margin: 0;
      font-size: clamp(28px, 3.4vw, 44px);
      font-weight: 850;
      letter-spacing: -.04em;
    }

    .brand-subtitle {
      color: var(--nasa-muted);
      font-size: 14px;
      margin-top: 3px;
    }

    .status-pill {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      border: 1px solid var(--nasa-border);
      border-radius: 999px;
      background: rgba(255, 255, 255, .055);
      color: var(--nasa-text);
      padding: 8px 12px;
      font-size: 13px;
      white-space: nowrap;
    }

    .status-dot {
      width: 8px;
      height: 8px;
      border-radius: 999px;
      background: var(--nasa-green);
      box-shadow: 0 0 14px var(--nasa-green);
    }

    .btn-ghost {
      border: 1px solid var(--nasa-border);
      background: rgba(255, 255, 255, .055);
      color: var(--nasa-text);
      border-radius: 12px;
    }

    .btn-ghost:hover {
      border-color: rgba(110, 231, 255, .38);
      color: var(--nasa-cyan);
      background: rgba(110, 231, 255, .08);
    }

    .btn-nasa {
      border: 0;
      background: linear-gradient(135deg, var(--nasa-blue), var(--nasa-cyan));
      color: #031018;
      font-weight: 800;
      border-radius: 12px;
      min-height: 40px;
      box-shadow: 0 0 22px rgba(110, 231, 255, .16);
    }

    .btn-nasa:hover {
      filter: brightness(1.05);
      color: #031018;
    }

    .nasa-tabs .nav-link {
      color: var(--nasa-muted);
      border: 1px solid transparent;
      border-radius: 999px;
      padding: 9px 15px;
    }

    .nasa-tabs .nav-link.active {
      background: rgba(110, 231, 255, .13);
      border-color: rgba(110, 231, 255, .28);
      color: var(--nasa-cyan);
    }

    .hero-grid {
      display: grid;
      grid-template-columns: 300px minmax(420px, 1fr) 320px;
      gap: 18px;
      align-items: stretch;
    }

    .content-grid {
      margin-top: 18px;
      display: grid;
      grid-template-columns: minmax(0, 1.35fr) minmax(340px, .8fr);
      gap: 18px;
      align-items: start;
    }

    .filters-card,
    .space-scene,
    .active-threat-card,
    .mission-status-card,
    .results-card,
    .charts-card,
    .detail-card {
      padding: 18px;
      min-width: 0;
    }

    .section-title {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      margin-bottom: 14px;
    }

    .section-title h2,
    .section-title h3 {
      margin: 0;
      font-size: 17px;
      font-weight: 800;
    }

    .section-title small {
      color: var(--nasa-muted);
    }

    .filters .row {
      --bs-gutter-y: .75rem;
    }

    .form-label {
      color: #cbd5e1;
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: .08em;
      font-weight: 750;
    }

    .form-control,
    .form-select {
      min-height: 38px;
      font-size: 13px;
      background-color: rgba(255, 255, 255, .06);
      border-color: rgba(255, 255, 255, .13);
      color: var(--nasa-text);
      border-radius: 12px;
    }

    .form-control:focus,
    .form-select:focus {
      background-color: rgba(255, 255, 255, .08);
      color: var(--nasa-text);
      border-color: rgba(110, 231, 255, .48);
      box-shadow: 0 0 0 .25rem rgba(110, 231, 255, .10);
    }

    .form-select option {
      background: #111827;
      color: #fff;
    }

    .metric-list {
      display: grid;
      gap: 12px;
    }

    .metric-card {
      padding: 16px;
      border-radius: 16px;
      background: rgba(255, 255, 255, .055);
      border: 1px solid rgba(255, 255, 255, .085);
    }

    .metric-card-primary {
      border-color: rgba(110, 231, 255, .18);
    }

    .metric-card-danger {
      border-color: rgba(239, 68, 68, .20);
    }

    .metric-card-source {
      border-color: rgba(34, 197, 94, .18);
    }

    .metric-label {
      color: var(--nasa-muted);
      font-size: 12px;
      margin-bottom: 8px;
    }

    .metric-value {
      font-size: 28px;
      line-height: 1;
      font-weight: 850;
      font-variant-numeric: tabular-nums;
    }

    .metric-value small {
      font-size: 13px;
      color: var(--nasa-muted);
      font-weight: 500;
    }

    .metric-trend {
      font-size: 12px;
      margin-top: 8px;
    }

    .trend-good {
      color: var(--nasa-green);
    }

    .trend-warn {
      color: var(--nasa-orange);
    }

    .trend-danger {
      color: var(--nasa-red);
    }

    .space-scene {
      position: relative;
      min-height: 390px;
      overflow: hidden;
      background:
        radial-gradient(circle at 60% 28%, rgba(255, 255, 255, .18), transparent 16%),
        radial-gradient(circle at 24% 70%, rgba(110, 231, 255, .09), transparent 22%),
        rgba(255, 255, 255, .035);
    }

    .scene-toolbar {
      position: relative;
      z-index: 20;
      display: flex;
      justify-content: space-between;
      gap: 12px;
      align-items: flex-start;
    }

    .scene-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      border: 1px solid rgba(110, 231, 255, .28);
      background: rgba(110, 231, 255, .09);
      color: var(--nasa-cyan);
      border-radius: 999px;
      padding: 7px 12px;
      font-size: 12px;
      white-space: nowrap;
    }

    .toolbar-actions {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: flex-end;
      gap: 8px;
    }

    .space-scene .toolbar-actions {
      max-width: 58%;
    }

    .space-scene .toolbar-actions .btn {
      white-space: nowrap;
    }

    .risk-badge {
      display: inline-flex;
      align-items: center;
      gap: 7px;
      border-radius: 999px;
      padding: 6px 12px;
      font-size: 12px;
      font-weight: 800;
      letter-spacing: .02em;
    }

    .risk-danger {
      background: rgba(239, 68, 68, .20);
      color: #fecaca;
      border: 1px solid rgba(239, 68, 68, .55);
      box-shadow: 0 0 18px rgba(239, 68, 68, .12);
    }

    .risk-safe {
      background: rgba(34, 197, 94, .18);
      color: #bbf7d0;
      border: 1px solid rgba(34, 197, 94, .45);
    }

    /* =========================================================
       Cinematic mission visualizer
       ========================================================= */

    .mission-visualizer {
      isolation: isolate;
    }

    .cinematic-visualizer {
      position: relative;
      min-height: 560px;
      overflow: hidden;
      background:
        radial-gradient(circle at 70% 18%, rgba(255,255,255,.20), transparent 10%),
        radial-gradient(circle at 28% 45%, rgba(110,231,255,.12), transparent 26%),
        radial-gradient(circle at 80% 85%, rgba(168,85,247,.13), transparent 28%),
        linear-gradient(135deg, #03060b 0%, #111827 52%, #07111f 100%);
    }

    .cinematic-stars {
      position: absolute;
      inset: 0;
      opacity: .48;
      background-image:
        radial-gradient(circle, rgba(255,255,255,.75) 0 1px, transparent 1.5px),
        radial-gradient(circle, rgba(110,231,255,.55) 0 1px, transparent 1.4px);
      background-size: 110px 110px, 180px 180px;
      background-position: 20px 40px, 90px 80px;
      z-index: 1;
    }

    .cinematic-sun {
      position: absolute;
      right: 18%;
      top: 4%;
      width: 150px;
      height: 150px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(255,255,255,.95), rgba(255,255,255,.32) 28%, transparent 68%);
      filter: blur(1px);
      box-shadow: 0 0 95px rgba(255,255,255,.45);
      opacity: .76;
      z-index: 2;
    }

    .cinematic-earth {
      position: absolute;
      left: -35px;
      bottom: -45px;
      width: 170px;
      height: 170px;
      border-radius: 50%;
      background:
        radial-gradient(circle at 60% 30%, rgba(255,255,255,.55), transparent 9%),
        radial-gradient(circle at 35% 45%, rgba(110,231,255,.22), transparent 22%),
        linear-gradient(135deg, #d1d5db, #334155 45%, #020617);
      box-shadow:
        inset -30px -25px 55px rgba(0,0,0,.82),
        0 0 55px rgba(110,231,255,.12);
      opacity: .72;
      z-index: 2;
    }

    .cinematic-asteroid {
      position: absolute;
      left: 50%;
      top: 43%;
      width: min(760px, 84%);
      height: 255px;
      transform: translate(-50%, -50%) rotate(-17deg);
      clip-path: polygon(
        2% 57%,
        10% 35%,
        28% 16%,
        48% 5%,
        76% 8%,
        96% 27%,
        91% 50%,
        73% 71%,
        47% 91%,
        20% 86%,
        6% 71%
      );
      background:
        radial-gradient(circle at 23% 48%, rgba(255,255,255,.80), transparent 7%),
        radial-gradient(circle at 44% 53%, rgba(0,0,0,.78), transparent 8%),
        radial-gradient(circle at 67% 37%, rgba(0,0,0,.72), transparent 9%),
        radial-gradient(circle at 72% 64%, rgba(255,255,255,.22), transparent 9%),
        radial-gradient(circle at 38% 76%, rgba(0,0,0,.65), transparent 9%),
        linear-gradient(135deg, #050505 0%, #202426 23%, #e5e7eb 47%, #555d60 64%, #090909 100%);
      box-shadow:
        inset -120px -42px 120px rgba(0,0,0,.92),
        inset 65px 25px 95px rgba(255,255,255,.28),
        0 36px 120px rgba(255,255,255,.18);
      filter: grayscale(.75) contrast(1.22);
      z-index: 3;
    }

    .cinematic-asteroid::before {
      content: "";
      position: absolute;
      inset: 0;
      background:
        linear-gradient(100deg, transparent 0 34%, rgba(255,255,255,.20) 42%, transparent 56%),
        radial-gradient(circle at 54% 48%, rgba(0,0,0,.55), transparent 9%),
        radial-gradient(circle at 75% 25%, rgba(0,0,0,.45), transparent 7%);
      opacity: .55;
    }

    .cinematic-asteroid .crater {
      position: absolute;
      border-radius: 50%;
      background: radial-gradient(circle at 38% 35%, rgba(255,255,255,.22), rgba(0,0,0,.72) 62%);
      box-shadow: inset 3px 3px 9px rgba(255,255,255,.12);
    }

    .crater-a {
      width: 44px;
      height: 44px;
      left: 37%;
      top: 48%;
    }

    .crater-b {
      width: 58px;
      height: 58px;
      left: 63%;
      top: 30%;
    }

    .crater-c {
      width: 38px;
      height: 38px;
      left: 27%;
      top: 58%;
    }

    .crater-d {
      width: 52px;
      height: 52px;
      left: 47%;
      top: 72%;
    }

    .scene-tech-label {
      position: absolute;
      z-index: 8;
      padding: 10px 12px;
      border-radius: 13px;
      background: rgba(3, 7, 18, .52);
      border: 1px solid rgba(255,255,255,.12);
      backdrop-filter: blur(10px);
      min-width: 132px;
    }

    .scene-tech-label span {
      display: block;
      color: var(--nasa-muted);
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: .08em;
      font-weight: 800;
    }

    .scene-tech-label strong {
      display: block;
      color: #fff;
      font-size: 13px;
      margin-top: 4px;
    }

    .label-risk {
      left: 7%;
      top: 47%;
    }

    .label-distance {
      right: 8%;
      bottom: 29%;
    }

    .label-size {
      top: 18%;
      left: 42%;
    }

    .tech-line {
      position: absolute;
      z-index: 7;
      height: 1px;
      background: rgba(255,255,255,.34);
      transform-origin: left center;
    }

    .line-risk {
      width: 150px;
      left: 18%;
      top: 52%;
      transform: rotate(-8deg);
    }

    .line-distance {
      width: 130px;
      right: 15%;
      bottom: 38%;
      transform: rotate(-18deg);
    }

    .line-size {
      width: 120px;
      left: 48%;
      top: 29%;
      transform: rotate(32deg);
    }

    .visualizer-bottom-panel {
      position: absolute;
      left: 24px;
      right: 24px;
      bottom: 18px;
      z-index: 9;
      display: grid;
      grid-template-columns: minmax(0, 1fr) 260px;
      gap: 14px;
      align-items: stretch;
    }

    .visualizer-bottom-panel > div {
      padding: 16px;
      border-radius: 18px;
      background: rgba(3, 7, 18, .62);
      border: 1px solid rgba(255,255,255,.12);
      backdrop-filter: blur(12px);
    }

    .visualizer-bottom-panel h3 {
      margin: 10px 0 6px;
      font-size: 22px;
      font-weight: 900;
    }

    .visualizer-bottom-panel p {
      margin: 0;
      color: var(--nasa-muted);
      font-size: 13px;
      line-height: 1.45;
    }

    .mission-energy {
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .mission-energy span {
      color: var(--nasa-muted);
      text-transform: uppercase;
      letter-spacing: .08em;
      font-size: 11px;
      font-weight: 800;
    }

    .mission-energy strong {
      display: block;
      margin-top: 8px;
      font-size: 22px;
      color: var(--nasa-cyan);
      font-weight: 900;
    }

    .mission-energy small {
      display: block;
      margin-top: 5px;
      color: var(--nasa-muted);
    }

    /* =========================================================
       Selected object compact
       ========================================================= */

    .selected-object-card {
      min-height: auto;
    }

    .selected-object-compact {
      display: grid;
      grid-template-columns: auto 1fr;
      gap: 12px;
      align-items: center;
      padding: 14px;
      border-radius: 16px;
      background: rgba(255,255,255,.055);
      border: 1px solid rgba(255,255,255,.08);
      margin-bottom: 14px;
    }

    .selected-object-icon {
      width: 48px;
      height: 48px;
      border-radius: 15px;
      display: grid;
      place-items: center;
      background:
        radial-gradient(circle at 35% 30%, rgba(255,255,255,.75), transparent 12%),
        linear-gradient(135deg, #d1d5db, #374151);
      color: #111827;
      transform: rotate(-12deg);
    }

    .selected-label {
      display: block;
      color: var(--nasa-muted);
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: .08em;
      font-weight: 800;
    }

    .selected-object-compact strong {
      display: block;
      font-size: 15px;
      font-weight: 900;
      color: var(--nasa-text);
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .selected-object-compact small {
      color: var(--nasa-muted);
    }

    .selected-object-actions {
      margin-bottom: 12px;
    }

    .selected-object-note {
      display: flex;
      gap: 8px;
      font-size: 12px;
      color: var(--nasa-muted);
      line-height: 1.45;
    }

    .right-stack {
      display: grid;
      gap: 18px;
    }

    .mission-status-box {
      display: grid;
      gap: 10px;
    }

    .mission-status-box div {
      display: flex;
      justify-content: space-between;
      gap: 12px;
      padding: 11px 12px;
      border-radius: 12px;
      background: rgba(255, 255, 255, .055);
    }

    .mission-status-box span {
      color: var(--nasa-muted);
    }

    .mission-status-box strong {
      color: var(--nasa-cyan);
    }

    .asteroid-list {
      display: grid;
      gap: 12px;
    }

    .asteroid-card {
      display: grid;
      grid-template-columns: auto 1fr auto;
      gap: 16px;
      align-items: center;
      padding: 17px 18px;
      border-radius: 18px;
      background: linear-gradient(90deg, rgba(255, 255, 255, .075), rgba(255, 255, 255, .035));
      border: 1px solid rgba(255, 255, 255, .11);
      transition: transform .18s ease, border-color .18s ease, background .18s ease, box-shadow .18s ease;
    }

    .asteroid-card:hover {
      transform: translateY(-2px);
      border-color: rgba(110, 231, 255, .35);
      background: linear-gradient(90deg, rgba(110, 231, 255, .10), rgba(255, 255, 255, .045));
    }

    .asteroid-card.selected {
      border-color: rgba(110, 231, 255, .65);
      box-shadow:
        0 0 0 1px rgba(110, 231, 255, .18),
        0 18px 48px rgba(110, 231, 255, .08);
    }

    .asteroid-card.dangerous {
      border-color: rgba(239, 68, 68, .45);
      background: linear-gradient(90deg, rgba(239, 68, 68, .14), rgba(255, 255, 255, .045));
    }

    .asteroid-icon {
      width: 54px;
      height: 54px;
      border-radius: 16px;
      display: grid;
      place-items: center;
      background:
        radial-gradient(circle at 35% 30%, rgba(255, 255, 255, .75), transparent 12%),
        linear-gradient(135deg, #d1d5db, #374151);
      color: #111827;
      filter: grayscale(.6);
      transform: rotate(-12deg);
    }

    .asteroid-title {
      display: flex;
      align-items: center;
      flex-wrap: wrap;
      gap: 8px;
      min-width: 0;
      margin-bottom: 5px;
    }

    .asteroid-title h4 {
      margin: 0;
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      font-size: 18px;
      font-weight: 850;
    }

    .asteroid-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      min-width: 0;
      color: var(--nasa-muted);
      font-size: 12px;
    }

    .detail-empty {
      min-height: 280px;
      display: grid;
      place-items: center;
      text-align: center;
      color: var(--nasa-muted);
      border-radius: 16px;
      border: 1px dashed rgba(255, 255, 255, .15);
      background: rgba(255, 255, 255, .035);
      padding: 24px;
    }

    .detail-panel {
      width: 100%;
    }

    .detail-panel h4 {
      color: #fff;
      font-size: 20px;
      font-weight: 850;
    }

    .detail-chip {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      margin-bottom: 14px;
      padding: 5px 10px;
      border-radius: 999px;
      background: rgba(110, 231, 255, .10);
      border: 1px solid rgba(110, 231, 255, .25);
      color: var(--nasa-cyan);
      font-size: 12px;
      font-weight: 750;
    }

    .detail-section-title {
      margin: 0 0 10px;
      color: #e5e7eb;
      font-size: 13px;
      text-transform: uppercase;
      letter-spacing: .08em;
      font-weight: 850;
    }

    .detail-kv {
      display: grid;
      gap: 8px;
    }

    .detail-kv div {
      display: flex;
      justify-content: space-between;
      gap: 12px;
      padding: 10px;
      border-radius: 12px;
      background: rgba(255, 255, 255, .055);
      border: 1px solid rgba(255, 255, 255, .06);
      font-size: 13px;
    }

    .detail-kv span:first-child {
      color: var(--nasa-muted);
    }

    .detail-kv strong {
      text-align: right;
      color: #e5e7eb;
    }

    .close-approach-list {
      display: grid;
      gap: 8px;
      margin: 0;
      padding: 0;
      list-style: none;
    }

    .close-approach-item {
      display: grid;
      gap: 3px;
      padding: 10px;
      border-radius: 12px;
      background: rgba(255, 255, 255, .055);
      border: 1px solid rgba(255, 255, 255, .06);
    }

    .close-approach-item strong {
      color: #e5e7eb;
      font-size: 13px;
    }

    .close-approach-item span {
      color: var(--nasa-muted);
      font-size: 12px;
    }

    .detail-loading {
      margin-top: 12px;
      color: var(--nasa-cyan);
      font-size: 13px;
    }

    .loader-overlay {
      position: fixed;
      left: 50%;
      bottom: 28px;
      transform: translateX(-50%);
      z-index: 2000;
      padding: 11px 18px;
      border-radius: 999px;
      background: rgba(3, 7, 18, .92);
      border: 1px solid var(--nasa-border-strong);
      color: var(--nasa-cyan);
      box-shadow: 0 10px 40px rgba(0, 0, 0, .32);
      display: none;
    }

    .loader-overlay.htmx-request {
      display: block;
    }

    .toast-target:not(:empty) {
      position: fixed;
      right: 24px;
      bottom: 24px;
      z-index: 2100;
      width: min(420px, calc(100% - 48px));
    }

    /* =========================================================
       Asteroid list pagination
       ========================================================= */

    .asteroid-list-toolbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 14px;
      margin-bottom: 14px;
      padding: 10px 12px;
      border-radius: 14px;
      background: rgba(255, 255, 255, .04);
      border: 1px solid rgba(255, 255, 255, .07);
    }

    .list-counter {
      display: inline-flex;
      align-items: center;
      color: var(--nasa-muted);
      font-size: 13px;
    }

    .list-counter strong {
      color: var(--nasa-text);
      font-weight: 800;
    }

    .page-size-control {
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    .page-size-control label {
      color: var(--nasa-muted);
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: .08em;
      font-weight: 800;
    }

    .page-size-control .form-select {
      width: 82px;
      min-height: 34px;
      padding-top: 4px;
      padding-bottom: 4px;
    }

    .asteroid-pagination {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
      margin-top: 16px;
      padding-top: 14px;
      border-top: 1px solid rgba(255, 255, 255, .08);
    }

    .pagination-pages {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 6px;
    }

    .pagination-page-btn {
      min-width: 34px;
      height: 34px;
      border-radius: 10px;
      border: 1px solid rgba(255, 255, 255, .10);
      background: rgba(255, 255, 255, .055);
      color: var(--nasa-text);
      font-size: 13px;
      font-weight: 750;
    }

    .pagination-page-btn:hover {
      border-color: rgba(110, 231, 255, .38);
      color: var(--nasa-cyan);
      background: rgba(110, 231, 255, .08);
    }

    .pagination-page-btn.active {
      background: linear-gradient(135deg, var(--nasa-blue), var(--nasa-cyan));
      color: #031018;
      border-color: transparent;
    }

    .asteroid-page-item.is-hidden {
      display: none;
    }

    /* =========================================================
       Recharts-like chart cards
       ========================================================= */

    .charts-dashboard {
      padding: 22px;
    }

    .charts-header {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 18px;
      margin-bottom: 22px;
    }

    .charts-header h2 {
      margin: 0 0 8px;
      font-size: 30px;
      font-weight: 900;
    }

    .charts-header p {
      margin: 0;
      color: var(--nasa-muted);
      max-width: 760px;
    }

    .charts-example-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 18px;
    }

    .chart-example-card {
      min-width: 0;
      overflow: hidden;
      border-radius: 18px;
      background: rgba(255, 255, 255, .035);
      border: 1px solid rgba(255, 255, 255, .13);
      box-shadow: 0 18px 50px rgba(0, 0, 0, .22);
    }

    .chart-example-body {
      height: 260px;
      padding: 14px;
      background:
        radial-gradient(circle at 50% 0%, rgba(110, 231, 255, .08), transparent 28%),
        rgba(0, 0, 0, .18);
      border-bottom: 1px solid rgba(255, 255, 255, .10);
    }

    .chart-example-body canvas {
      width: 100% !important;
      height: 100% !important;
    }

    .chart-example-footer {
      min-height: 72px;
      padding: 16px 18px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 14px;
    }

    .chart-example-footer h3 {
      margin: 0 0 4px;
      font-size: 16px;
      font-weight: 900;
    }

    .chart-example-footer small {
      color: var(--nasa-muted);
    }

    .chart-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      border-radius: 999px;
      padding: 7px 10px;
      background: rgba(110, 231, 255, .10);
      border: 1px solid rgba(110, 231, 255, .24);
      color: var(--nasa-cyan);
      font-size: 11px;
      font-weight: 850;
      white-space: nowrap;
    }

    .chart-badge.warning {
      background: rgba(245, 158, 11, .12);
      border-color: rgba(245, 158, 11, .28);
      color: #fbbf24;
    }

    @media (max-width: 1450px) {
      .hero-grid {
        grid-template-columns: 300px minmax(0, 1fr);
      }

      .right-stack {
        grid-column: 1 / -1;
        grid-template-columns: 1fr 1fr;
      }

      .content-grid {
        grid-template-columns: 1fr;
      }

      .space-scene .toolbar-actions {
        max-width: 48%;
      }

      .space-scene .toolbar-actions .btn {
        font-size: 12px;
        padding: 6px 9px;
      }
    }

    @media (max-width: 1250px) {
      .charts-example-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }

    @media (max-width: 1200px) {
      .cinematic-visualizer {
        min-height: 620px;
      }

      .visualizer-bottom-panel {
        grid-template-columns: 1fr;
      }

      .cinematic-asteroid {
        width: 88%;
        height: 225px;
      }
    }

    @media (max-width: 900px) {
      body {
        padding: 14px;
      }

      .dashboard-header,
      .nav {
        flex-wrap: wrap;
      }

      .hero-grid,
      .right-stack,
      .content-grid {
        grid-template-columns: 1fr;
      }

      .space-scene .toolbar-actions {
        max-width: 100%;
        justify-content: flex-start;
      }

      .asteroid-card {
        grid-template-columns: auto 1fr;
      }

      .asteroid-card .btn {
        grid-column: 1 / -1;
      }
    }

    @media (max-width: 800px) {
      .scene-tech-label,
      .tech-line {
        display: none;
      }

      .cinematic-visualizer {
        min-height: 560px;
      }

      .cinematic-asteroid {
        width: 96%;
        height: 190px;
      }
    }

    @media (max-width: 780px) {
      .charts-header {
        flex-direction: column;
      }

      .charts-example-grid {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 700px) {
      .asteroid-list-toolbar,
      .asteroid-pagination {
        align-items: stretch;
        flex-direction: column;
      }

      .pagination-pages {
        order: -1;
      }

      .page-size-control {
        justify-content: space-between;
      }
    }
  </style>
</head>

<body>
  <div class="app-shell">

    <!-- HEADER -->
    <header class="dashboard-header">
      <div class="brand-block">
        <div class="brand-logo">
          <i class="fa-solid fa-meteor"></i>
        </div>

        <div>
          <h1 class="brand-title">Asteroid Monitoring</h1>
          <div class="brand-subtitle">
            Real-time view of near-Earth asteroids, risk level and close approach data.
          </div>
        </div>
      </div>

      <div class="d-flex align-items-center flex-wrap gap-2">
        <span class="status-pill">
          <span class="status-dot"></span>
          Live data
        </span>

        <span class="status-pill">
          <i class="fa-solid fa-meteor text-info"></i>
          NASA NeoWs
        </span>

        <span class="status-pill">
          <i class="fa-solid fa-triangle-exclamation text-warning"></i>
          Risk monitor
        </span>

        <button class="btn btn-sm btn-ghost" type="button" data-bs-toggle="modal" data-bs-target="#apiInfoModal">
          <i class="fa-solid fa-circle-info me-1"></i>
          About data
        </button>
      </div>
    </header>

    <!-- MAIN TABS -->
    <ul class="nav nasa-tabs mb-3" id="mainTab" role="tablist">

      <li class="nav-item" role="presentation">
        <button class="nav-link active" id="monitor-tab" data-bs-toggle="tab" data-bs-target="#monitor" type="button"
          role="tab">
          <i class="fa-solid fa-satellite-dish me-1"></i>
          Monitoring
        </button>
      </li>

      <li class="nav-item" role="presentation">
        <button class="nav-link" id="charts-tab" data-bs-toggle="tab" data-bs-target="#charts" type="button" role="tab">
          <i class="fa-solid fa-chart-line me-1"></i>
          Charts
        </button>
      </li>

    </ul>

    <div class="tab-content" id="mainTabContent">

      <!-- TAB MONITORING -->
      <div class="tab-pane fade show active" id="monitor" role="tabpanel" aria-labelledby="monitor-tab">
        <% monitoringHtml %>
      </div>

      <!-- TAB CHARTS -->
      <div class="tab-pane fade" id="charts" role="tabpanel" aria-labelledby="charts-tab">
        <% chartsHtml %>
      </div>

    </div>

    <!-- MODALE DATA INFO -->
    <div class="modal fade" id="apiInfoModal" tabindex="-1" aria-labelledby="apiInfoModalLabel" aria-hidden="true">
      <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content bg-dark text-light border-secondary">
          <div class="modal-header">
            <h5 class="modal-title" id="apiInfoModalLabel">
              <i class="fa-solid fa-circle-info me-2"></i>
              About the data
            </h5>

            <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>

          <div class="modal-body">
            <p>
              This dashboard tracks near-Earth objects using NASA NeoWs data.
              Each object includes close approach information, estimated diameter, speed, miss distance and risk
              classification.
            </p>

            <p class="mb-0 text-secondary">
              The dashboard focuses on the monitoring experience: filtering, risk visibility and quick access to
              asteroid details.
            </p>
          </div>
        </div>
      </div>
    </div>

    <div id="globalLoader" class="loader-overlay htmx-indicator">
      <i class="fa-solid fa-spinner fa-spin me-2"></i>
      Loading...
    </div>

    <div id="toast-target" class="toast-target"></div>

  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://unpkg.com/htmx.org@1.9.12"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.10.8/dist/sweetalert2.all.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.7/dist/chart.umd.min.js"></script>

  <script>
    window.NEOWatchPagination = (function () {
      let currentPage = 1;
      let pageSize = 5;

      function getItems() {
        return Array.from(document.querySelectorAll('#asteroidList .asteroid-page-item'));
      }

      function getTotalPages(items) {
        if (!items.length) {
          return 1;
        }

        return Math.max(1, Math.ceil(items.length / pageSize));
      }

      function updateVisibleRange(items) {
        const range = document.getElementById('asteroidVisibleRange');

        if (!range) {
          return;
        }

        if (!items.length) {
          range.textContent = 'No objects';
          return;
        }

        const start = ((currentPage - 1) * pageSize) + 1;
        const end = Math.min(currentPage * pageSize, items.length);

        range.textContent = `${start}-${end} of ${items.length} objects`;
      }

      function renderPageButtons(totalPages) {
        const container = document.getElementById('asteroidPaginationPages');

        if (!container) {
          return;
        }

        container.innerHTML = '';

        for (let index = 1; index <= totalPages; index++) {
          const button = document.createElement('button');

          button.type = 'button';
          button.className = 'pagination-page-btn' + (index === currentPage ? ' active' : '');
          button.textContent = index;

          button.addEventListener('click', function () {
            currentPage = index;
            render();
          });

          container.appendChild(button);
        }
      }

      function render() {
        const items = getItems();

        if (!items.length) {
          return;
        }

        const totalPages = getTotalPages(items);

        if (currentPage > totalPages) {
          currentPage = totalPages;
        }

        if (currentPage < 1) {
          currentPage = 1;
        }

        items.forEach(function (item, index) {
          const startIndex = (currentPage - 1) * pageSize;
          const endIndex = startIndex + pageSize;

          if (index >= startIndex && index < endIndex) {
            item.classList.remove('is-hidden');
          } else {
            item.classList.add('is-hidden');
          }
        });

        updateVisibleRange(items);
        renderPageButtons(totalPages);
      }

      function initialize() {
        currentPage = 1;

        const pageSizeSelect = document.getElementById('asteroidPageSize');

        if (pageSizeSelect) {
          pageSize = parseInt(pageSizeSelect.value || '5', 10);
        }

        render();
      }

      function changePageSize(value) {
        pageSize = parseInt(value || '5', 10);
        currentPage = 1;
        render();
      }

      function nextPage() {
        const items = getItems();
        const totalPages = getTotalPages(items);

        if (currentPage < totalPages) {
          currentPage++;
          render();
        }
      }

      function previousPage() {
        if (currentPage > 1) {
          currentPage--;
          render();
        }
      }

      return {
        initialize: initialize,
        changePageSize: changePageSize,
        nextPage: nextPage,
        previousPage: previousPage
      };
    })();

    document.addEventListener('DOMContentLoaded', function () {
      window.NEOWatchPagination.initialize();
    });

    document.body.addEventListener('htmx:afterSwap', function (event) {
      if (event.detail && event.detail.target && event.detail.target.id === 'monitoringContainer') {
        window.NEOWatchPagination.initialize();
      }
    });
  </script>

  <script>
    window.NEOWatchSelection = (function () {
      function setText(id, value) {
        const element = document.getElementById(id);

        if (element) {
          element.textContent = value;
        }
      }

      function setRiskBadge(id, riskClass, riskLabel) {
        const element = document.getElementById(id);

        if (!element) {
          return;
        }

        element.className = riskClass || 'risk-badge risk-safe';
        element.innerHTML = '<i class="fa-solid fa-circle"></i> ' + (riskLabel || 'Unknown');
      }

      function selectCard(button) {
        document.querySelectorAll('.asteroid-card').forEach(function (card) {
          card.classList.remove('selected');
        });

        const card = button.closest('.asteroid-card');

        if (card) {
          card.classList.add('selected');
        }
      }

      function updateActiveDetailsButton(id) {
        const button = document.getElementById('activeDetailsButton');

        if (!button || !id) {
          return;
        }

        button.disabled = false;
        button.textContent = 'Open full details';
        button.setAttribute('hx-get', '/asteroids/' + id);

        if (window.htmx) {
          htmx.process(button);
        }
      }

      function parseNumber(value) {
        if (!value) {
          return 0;
        }

        return Number(
          String(value)
            .replace(/[^\d,.-]/g, '')
            .replace(/\./g, '')
            .replace(',', '.')
        ) || 0;
      }

      function getDistanceClass(distanceText) {
        const distance = parseNumber(distanceText);

        if (distance <= 7500000) {
          return 'Close range';
        }

        if (distance <= 20000000) {
          return 'Medium range';
        }

        return 'Long range';
      }

      function getVelocityClass(velocityText) {
        const velocity = parseNumber(velocityText);

        if (velocity >= 70000) {
          return 'High speed';
        }

        if (velocity >= 35000) {
          return 'Medium speed';
        }

        return 'Low speed';
      }

      function getSizeClass(diameterText) {
        const size = parseNumber(diameterText);

        if (size >= 500) {
          return 'Large object';
        }

        if (size >= 100) {
          return 'Medium object';
        }

        return 'Small object';
      }

      function getRiskProfile(riskLabel) {
        const label = (riskLabel || '').toLowerCase();

        if (label.includes('danger') || label.includes('hazard')) {
          return 'Potential threat';
        }

        return 'Safe object';
      }

      function updateFromButton(button) {
        const id = button.dataset.id || '';
        const name = button.dataset.name || '--';
        const distance = button.dataset.distance || '-- km';
        const velocity = button.dataset.velocity || '-- km/h';
        const diameter = button.dataset.diameter || '-- m';
        const riskClass = button.dataset.riskClass || 'risk-badge risk-safe';
        const riskLabel = button.dataset.riskLabel || 'Unknown';
        const sizeClass = button.dataset.sizeClass || '--';

        selectCard(button);

        setRiskBadge('activeRisk', riskClass, riskLabel);
        setText('activeObject', name);
        setText('activeObjectId', 'NASA object ID: ' + id);
        updateActiveDetailsButton(id);

        setRiskBadge('visualRisk', riskClass, riskLabel);
        setText('visualName', name);
        setText(
          'visualDescription',
          'Selected NASA NeoWs object. The panel translates raw values into a mission-style risk profile.'
        );

        setText('visualRiskProfile', getRiskProfile(riskLabel));
        setText('visualDistanceClass', getDistanceClass(distance));
        setText('visualVelocityClass', getVelocityClass(velocity));
        setText('visualSizeClass', sizeClass);
        setText('visualVelocity', velocity);
      }

      return {
        updateFromButton: updateFromButton
      };
    })();
  </script>

  <script>
    window.NEOWatchCharts = (function () {
      let charts = [];

      function destroyCharts() {
        charts.forEach(function (chart) {
          if (chart) {
            chart.destroy();
          }
        });

        charts = [];
      }

      function readData() {
        const element = document.getElementById('asteroidChartData');

        if (!element) {
          console.warn('Missing asteroidChartData element.');
          return [];
        }

        const raw = element.textContent || '[]';

        try {
          const data = JSON.parse(raw).filter(function (item) {
            return item && item.name;
          });

          return data;
        } catch (error) {
          console.error('Invalid asteroid chart data:', error);
          console.log('Raw asteroidChartData:', raw);
          return [];
        }
      }

      function colors() {
        return {
          blue: 'rgba(129, 124, 216, .92)',
          blueSoft: 'rgba(129, 124, 216, .26)',
          cyan: 'rgba(110, 231, 255, .92)',
          green: 'rgba(74, 222, 128, .88)',
          greenSoft: 'rgba(74, 222, 128, .18)',
          red: 'rgba(239, 68, 68, .88)',
          orange: 'rgba(245, 158, 11, .88)',
          grid: 'rgba(255,255,255,.20)',
          gridSoft: 'rgba(255,255,255,.08)',
          label: '#dbeafe'
        };
      }

      function compact(value) {
        return new Intl.NumberFormat('en-US', {
          notation: 'compact',
          maximumFractionDigits: 1
        }).format(value);
      }

      function baseOptions() {
        const c = colors();

        return {
          responsive: true,
          maintainAspectRatio: false,
          plugins: {
            legend: {
              labels: {
                color: c.label,
                usePointStyle: true,
                boxWidth: 8
              }
            },
            tooltip: {
              backgroundColor: 'rgba(3,7,18,.96)',
              borderColor: 'rgba(110,231,255,.25)',
              borderWidth: 1,
              titleColor: '#fff',
              bodyColor: c.label
            }
          },
          scales: {
            x: {
              ticks: {
                color: c.label,
                maxRotation: 0
              },
              grid: {
                color: c.gridSoft
              }
            },
            y: {
              ticks: {
                color: c.label,
                callback: function (value) {
                  return compact(value);
                }
              },
              grid: {
                color: c.grid
              }
            }
          }
        };
      }

      function createChart(canvasId, config) {
        const canvas = document.getElementById(canvasId);

        if (!canvas) {
          return;
        }

        charts.push(new Chart(canvas, config));
      }

      function renderDistanceChart(data) {
        const c = colors();

        const top = data
          .slice()
          .sort(function (a, b) { return a.distance - b.distance; })
          .slice(0, 8);

        createChart('distanceChart', {
          type: 'line',
          data: {
            labels: top.map(function (x) { return x.name; }),
            datasets: [
              {
                label: 'Distance',
                data: top.map(function (x) { return x.distance; }),
                borderColor: c.blue,
                backgroundColor: c.blueSoft,
                pointBackgroundColor: c.blue,
                pointBorderColor: '#050816',
                pointRadius: 4,
                tension: .38,
                fill: true,
                borderWidth: 2
              }
            ]
          },
          options: baseOptions()
        });
      }

      function renderDiameterChart(data) {
        const c = colors();

        const top = data
          .slice()
          .sort(function (a, b) { return b.diameter - a.diameter; })
          .slice(0, 8);

        createChart('diameterChart', {
          type: 'bar',
          data: {
            labels: top.map(function (x) { return x.name; }),
            datasets: [{
              label: 'Diameter',
              data: top.map(function (x) { return x.diameter; }),
              backgroundColor: top.map(function (x) {
                return x.hazardous ? c.red : c.blue;
              }),
              borderColor: top.map(function (x) {
                return x.hazardous ? c.red : c.cyan;
              }),
              borderWidth: 1,
              borderRadius: 8,
              maxBarThickness: 42
            }]
          },
          options: baseOptions()
        });
      }

      function renderRiskChart(data) {
        const c = colors();

        const hazardous = data.filter(function (x) {
          return x.hazardous === true;
        }).length;

        const safe = data.length - hazardous;

        createChart('riskChart', {
          type: 'doughnut',
          data: {
            labels: ['Safe', 'Potentially hazardous'],
            datasets: [{
              data: [safe, hazardous],
              backgroundColor: [c.green, c.red],
              borderColor: ['rgba(74,222,128,1)', 'rgba(239,68,68,1)'],
              borderWidth: 1,
              hoverOffset: 10
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '66%',
            plugins: {
              legend: {
                position: 'bottom',
                labels: {
                  color: c.label,
                  usePointStyle: true,
                  padding: 14
                }
              },
              tooltip: {
                backgroundColor: 'rgba(3,7,18,.96)',
                borderColor: 'rgba(110,231,255,.25)',
                borderWidth: 1,
                titleColor: '#fff',
                bodyColor: c.label
              }
            }
          }
        });
      }

      function renderVelocityChart(data) {
        const c = colors();

        const top = data
          .slice()
          .sort(function (a, b) { return b.velocity - a.velocity; })
          .slice(0, 8);

        createChart('velocityChart', {
          type: 'line',
          data: {
            labels: top.map(function (x) { return x.name; }),
            datasets: [{
              label: 'Velocity',
              data: top.map(function (x) { return x.velocity; }),
              borderColor: c.orange,
              backgroundColor: 'rgba(245,158,11,.18)',
              pointBackgroundColor: c.orange,
              pointBorderColor: '#050816',
              pointRadius: 5,
              tension: .36,
              fill: true,
              borderWidth: 2
            }]
          },
          options: baseOptions()
        });
      }

      function renderScatterChart(data) {
        const c = colors();

        createChart('scatterChart', {
          type: 'scatter',
          data: {
            datasets: [{
              label: 'Distance vs size',
              data: data.map(function (x) {
                return {
                  x: x.diameter,
                  y: x.distance
                };
              }),
              backgroundColor: data.map(function (x) {
                return x.hazardous ? c.red : c.blue;
              }),
              pointRadius: 6,
              pointHoverRadius: 8
            }]
          },
          options: baseOptions()
        });
      }

      function renderHazardBarChart(data) {
        const c = colors();

        const top = data
          .slice()
          .sort(function (a, b) { return b.diameter - a.diameter; })
          .slice(0, 8);

        createChart('hazardBarChart', {
          type: 'bar',
          data: {
            labels: top.map(function (x) { return x.name; }),
            datasets: [{
              label: 'Diameter',
              data: top.map(function (x) { return x.diameter; }),
              backgroundColor: top.map(function (x) {
                return x.hazardous ? c.red : c.blue;
              }),
              borderRadius: 8,
              maxBarThickness: 44
            }]
          },
          options: baseOptions()
        });
      }

      function render() {
        if (!window.Chart) {
          console.warn('Chart.js is not loaded.');
          return;
        }

        const data = readData();

        destroyCharts();

        if (!data.length) {
          console.warn('No real asteroid chart data available.');
          return;
        }

        renderDistanceChart(data);
        renderDiameterChart(data);
        renderRiskChart(data);
        renderVelocityChart(data);
        renderScatterChart(data);
        renderHazardBarChart(data);
      }

      return {
        render: render
      };
    })();

    document.addEventListener('DOMContentLoaded', function () {
      if (window.NEOWatchPagination) {
        window.NEOWatchPagination.initialize();
      }
    });

    document.body.addEventListener('shown.bs.tab', function (event) {
      if (event.target && event.target.id === 'charts-tab') {
        setTimeout(function () {
          window.NEOWatchCharts.render();
        }, 150);
      }
    });

    document.body.addEventListener('htmx:afterSwap', function (event) {
      if (event.detail && event.detail.target && event.detail.target.id === 'monitoringContainer') {
        if (window.NEOWatchPagination) {
          window.NEOWatchPagination.initialize();
        }

        setTimeout(function () {
          window.NEOWatchCharts.render();
        }, 150);
      }
    });
  </script>

</body>

</html>