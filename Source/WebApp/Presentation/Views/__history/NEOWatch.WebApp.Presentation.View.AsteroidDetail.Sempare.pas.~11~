unit NEOWatch.WebApp.Presentation.View.AsteroidDetail.Sempare;

interface

uses
  Spring.Logging,
  Fido.Utilities,
  NEOWatch.WebApp.Presentation.View.AsteroidDetail.Intf,
  NEOWatch.WebApp.Presentation.Model.DTO.WSAsteroidDetails;

type

  TViewAsteroidDetailSempare = class(TInterfacedObject, IViewAsteroidDetail)
  private
    const
      TemplateFileNameAsteroidDetail: array[0..2] of string = ('Partials', 'Monitoring', 'AsteroidDetail.html');
  private
    FLogger: ILogger;
    procedure HandleTemplateError(const AMessage: string);
  public
    constructor Create(const Logger: ILogger);
    function Render(const AsteroidDetail: TWSAsteroidDetailDTO): string;
  end;

implementation

constructor TViewAsteroidDetailSempare.Create(const Logger: ILogger);
begin
  inherited Create;
  FLogger := Utilities.CheckNotNullAndSet(Logger, 'Logger');
end;

procedure TViewAsteroidDetailSempare.HandleTemplateError(const AMessage: string);
begin
  FLogger.Error('Error parsing Sempare template "%s": %s', [TPath.Combine(TemplateFileNameMonitoring), AMessage]);
end;

function TViewAsteroidDetailSempare.Render(const AsteroidDetail: TWSAsteroidDetailDTO): string;
begin
  // TODO -cMM: TViewAsteroidDetailSempare.Render default body inserted
end;

end.
