unit NEOWatch.WebApp.Presentation.Model.Monitoring;

interface

uses
  NEOWatch.WebApp.Presentation.Model.DTO.WSAsteroidFilters,
  NEOWatch.WebApp.Presentation.Model.DTO.WSMonitoring,
  NEOWatch.WebApp.Presentation.Model.Monitoring.Intf,
  Fido.Mappers,
  Fido.Utilities,
  System.Generics.Collections,
  NEOWatch.WebApp.Presentation.Model.DTO.WSAsteroidCard,
  NEOWatch.WebApp.Domain.Entity.Monitoring.Intf,
  System.SysUtils,
  NEOWatch.WebApp.Domain.MonitoringRepository.Intf;

type

  TModelMonitoring = class(TInterfacedObject, IModelMonitoring)
  private
    FMonitoringRepository: IMonitoringRepository;
  public
    function GetAsteroidsList(const Value: TWSAsteroidFiltersDTO): TObjectList<TWSAsteroidCardDTO>;
    function GetDefaultList: TWSMonitoringDTO;
    function GetListByFilters(const Value: TWSAsteroidFiltersDTO): TWSMonitoringDTO;

    constructor Create(const MonitoringRepository: IMonitoringRepository);
  end;

implementation

constructor TModelMonitoring.Create(const MonitoringRepository: IMonitoringRepository);
begin
  inherited Create;
  FMonitoringRepository := Utilities.CheckNotNullAndSet(MonitoringRepository, 'MonitoringRepository');
end;

function TModelMonitoring.GetAsteroidsList(const Value: TWSAsteroidFiltersDTO): TObjectList<TWSAsteroidCardDTO>;
begin
  // TODO -cMM: TModelMonitoring.GetAsteroidsList default body inserted
end;

function TModelMonitoring.GetDefaultList: TWSMonitoringDTO;
var
  Monitoring: IMonitoring;
begin
  Result := TWSMonitoringDTO.Create;

  try
    Monitoring := FMonitoringRepository.GetDefaultList;

    Mappers.Map(Monitoring, Result);

  except
    on E: Exception do begin
      Result.HasError := True;
      Result.ErrorMessage := E.Message;

      Result.Summary.HasResults := False;
      Result.Summary.TotalCount := 0;
      Result.Summary.TotalCountText := '0';
      Result.Summary.SummaryMessage := 'Errore durante il caricamento degli asteroidi';
      Result.Summary.SummaryTrendClass := 'trend-danger';
    end;
  end;

end;

function TModelMonitoring.GetListByFilters(const Value: TWSAsteroidFiltersDTO): TWSMonitoringDTO;
begin
  // TODO -cMM: TModelMonitoring.GetListByFilters default body inserted
end;

initialization
  Mappers.RegisterMapper<IMonitoring, TWSMonitoringDTO>(
      procedure(const Source: IMonitoring; var Destination: TWSMonitoringDTO)
      begin
        //        Destination.Filters := Source.Filters;
        //        Destination.Asteroids := Source.Asteroids;
      end
  );

end.
